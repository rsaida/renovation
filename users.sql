CREATE TABLE accounts (
    email VARCHAR(100) NOT NULL,  -- Path to a file or directory
    password VARCHAR(100) NOT NULL  -- Display this in the overall projects (1/0)
);

INSERT INTO accounts (email, password)
VALUES 
     ('email', '123');